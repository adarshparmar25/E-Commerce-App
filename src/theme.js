import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Blue shade
    },
    secondary: {
      main: "#ff9800", // Orange shade for badge
    },
  },
});

export default theme;
